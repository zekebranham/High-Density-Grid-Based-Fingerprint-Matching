import cv2 as cv
import numpy as np
import math
import os

def process_grids (img, mask, high_density_grid_ids, term_positions, bif_positions):
    #furhter process hig-density grids to extract minutiae data
    return 